const labelWidth = 100
const valueWidth = 130
const percentWidth = 100
const pagination = {size: 'small',pageSize: 10,pageSlot: 8}

// 移动端分页配置（每页显示更少）
const mobilePagination = {size: 'small',pageSize: 5,pageSlot: 5}

export {
    labelWidth,
    valueWidth,
    percentWidth,
    pagination,
    mobilePagination
};