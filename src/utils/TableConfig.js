const labelWidth = 100
const valueWidth = 130
const percentWidth = 100
const pagination = {size: 'small',pageSize: 10,pageSlot: 8}

export {
    labelWidth,
    valueWidth,
    percentWidth,
    pagination
};